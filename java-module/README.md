# Java Engine Module From Scratch

...

## Naming Guidelines

A `java-engine` module must be named after the domain that it covers.

Also, its main class should be names like `Audio.java` if the module's name is `audio`.
