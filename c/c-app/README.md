# Write a desktop app in C and GTK 3 from scratch

https://danielc.dev/blog/tiny-windows-linux-gtk-apps/

## Notes

Use `file` to see the header description of an executable binary file: `file a.out`

Use `hexdump` to print the hexadecimal representation of a file: `hexdump a.out`

Use `gcc -S main.c` to look at the Assembly code.
